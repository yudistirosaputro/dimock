package com.yudistirosaputro.dimock.okhttp

/**
 * Same public shape as the debug artifact. The release artifact shows no inspector, so this value is
 * carried for source compatibility only.
 */
enum class InspectorTheme { System, Dark, Light }
