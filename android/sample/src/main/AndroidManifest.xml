<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Android 13+: the inspector is opened from dimock's notification. Requested once in MainActivity. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="dimock sample"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:usesCleartextTraffic="false">

        <!-- dimock starts itself (DimockInitProvider) in the debug artifact. Optional overrides:
             <meta-data android:name="com.yudistirosaputro.dimock.PORT" android:value="6767" />
             <meta-data android:name="com.yudistirosaputro.dimock.AUTO_INIT" android:value="false" />   then call Dimock.init yourself -->
        <meta-data android:name="com.yudistirosaputro.dimock.SHAKE_TO_OPEN" android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
